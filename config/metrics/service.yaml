apiVersion: v1
kind: Service
metadata:
  name: forge-controller-metrics
  namespace: forge-system
  labels:
    app: forge-controller
    component: metrics
spec:
  ports:
  - name: metrics
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: forge-controller
  type: ClusterIP
