apiVersion: v1
kind: ServiceAccount
metadata:
  name: forge-controller
  namespace: forge-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: forge-controller-role
rules:
# Permissions for ZarfPackage resources
- apiGroups:
  - zarf.dev
  resources:
  - zarfpackages
  verbs:
  - get
  - list
  - watch
# Permissions to update ZarfPackage status
- apiGroups:
  - zarf.dev
  resources:
  - zarfpackages/status
  verbs:
  - get
  - update
  - patch
# Permissions for UDSBundle resources
- apiGroups:
  - uds.io
  resources:
  - udsbundles
  verbs:
  - get
  - list
  - watch
# Permissions to update UDSBundle status
- apiGroups:
  - uds.io
  resources:
  - udsbundles/status
  verbs:
  - get
  - update
  - patch
# Permissions to read ServiceAccounts for policy validation
- apiGroups:
  - ""
  resources:
  - serviceaccounts
  verbs:
  - get
  - list
  - watch
# Permissions to read Secrets for credential validation
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - get
  - list
# Permissions for Jobs - create and read only (cleanup handled by TTL and owner references)
- apiGroups:
  - batch
  resources:
  - jobs
  verbs:
  - create
  - get
  - list
  - watch
# Permissions for Pods - read only (for debugging/status checking)
- apiGroups:
  - ""
  resources:
  - pods
  verbs:
  - get
  - list
  - watch
# Permissions for Events - create for better observability
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: forge-controller-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: forge-controller-role
subjects:
- kind: ServiceAccount
  name: forge-controller
  namespace: forge-system
