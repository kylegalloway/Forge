---
# Example: Deploy a pre-built package from OCI registry
apiVersion: zarf.dev/v1alpha1
kind: ZarfPackage
metadata:
  name: deploy-prebuilt-example
  namespace: production
spec:
  # ServiceAccount controls what this package is allowed to do
  # Created by cluster admin with forge.zarf.dev/* annotations
  serviceAccountName: developer-forge-sa
  action: Deploy

  # Source: Pre-built package in OCI registry
  source:
    type: OCI
    oci:
      image: ghcr.io/defenseunicorns/packages/init:v0.66.0
      credentialsSecretRef:
        name: ghcr-pull-secret

  # Deploy configuration
  deploy:
    target: InCluster
    namespace: zarf
    timeout: 15m
    components:
      - zarf-seed-registry
      - zarf-registry
      - zarf-agent
