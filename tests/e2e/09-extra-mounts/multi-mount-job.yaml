apiVersion: forge.dev/v1alpha3
kind: ZarfPackageJob
metadata:
  name: test-multi-mount
  namespace: default
  labels:
    app: forge-test
    test: e2e
    feature: extra-mounts
spec:
  serviceAccountName: test-extra-mounts
  action: Build
  source:
    type: Git
    git:
      url: https://github.com/kylegalloway/forge
      ref: main
      path: examples/samples/zarf/03-git-build-deploy

  # Extra mounts - mount both ConfigMap and Secret
  extraMounts:
    - source:
        type: ConfigMap
        configMap:
          name: test-app-config
      destination: /workspace/config
    - source:
        type: Secret
        secret:
          name: test-app-secrets
      destination: /workspace/secrets
